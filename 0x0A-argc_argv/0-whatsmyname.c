#include "main.h"
#include <stdio.h>
/**
  * main - checks code
  * @argc: argument count
  * @argv: array of strings
  * Return: always 0
  */
int main(int argc, char *argv[])
{
	if (argc == 1)
		printf("%s\n", argv[0]);
	return (0);
}
