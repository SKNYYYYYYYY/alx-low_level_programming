#include "main.h"
#include <stdio.h> // Don't forget to include the necessary header file for printf

/**
 *  * _puts - prints a string, followed by a new line, to stdout
 *   * @str: string to print
 *    */
void _puts(char *str)
{
	    printf("%s\n", str);
}
